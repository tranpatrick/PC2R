#include <stdlib.h>
#include <stdio.h>
#include "../include/clientlist.h"


int main(){
  /*  client_list *l = create_client_list(10, 1, NULL);
  printf("nbClients = %d\n", client_list_length(l));
  print_client_list(l);
  printf("----------------\n");
  add_client(l, 35, 2);
  add_name_client(l, 35, "pat");
  print_client_list(l);
  printf("----------------\n");
  add_client(l, 36, 3);
  add_name_client(l, 36, "ladi");
  add_client(l, 37, 4);
  add_name_client(l, 37, "vic");
  add_client(l, 38, 5);
  add_name_client(l, 38, "mouss");
  printf("nbTids = %d\n", client_list_length(l));
  print_client_list(l);
  printf("----------------\n");
  suppr_client(l, 37);
  print_client_list(l);
  printf("nbTids = %d\n", client_list_length(l));
  destroy_client_list(l); */
  return 0;
}
